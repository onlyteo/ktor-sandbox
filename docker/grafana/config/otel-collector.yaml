receivers:
  otlp:
    protocols:
      grpc:
exporters:
  otlp:
    endpoint: sandbox.grafana-tempo:4317
    tls:
      insecure: true
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
